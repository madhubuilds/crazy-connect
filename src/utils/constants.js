export const ROOM_SIZE = 12;
export const HALF_ROOM_SIZE = ROOM_SIZE / 2;

export const WALL_HEIGHT = 5;
export const WALL_THICKNESS = 0.2;

export const FLOOR_COLOR = "#c8ad7f"; // warmer, cozy
export const WALL_COLOR = "#f4ede4"; // soft cream

/**
 * Instead of hardcoding 12, 5, and 0.2 everywhere, we keep them in one place.
 * Room size: 14 × 14
    Wall height: 4
    Wall thickness: 0.15 
 */
